node_modules
package-lock.json

# Prose is wrapped by hand and the READMEs carry an ASCII diagram that
# reflowing would break. Markdown is reviewed, not formatted.
*.md

# A fixture file: the cases are grouped for reading, not for a formatter.
evals/cases.json
